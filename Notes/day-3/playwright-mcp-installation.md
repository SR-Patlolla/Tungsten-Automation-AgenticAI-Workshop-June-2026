# Playwright MCP installation

## Prerequisite
- VS code

## Steps to install MCP
- Open [Playwright Official Documentation](https://playwright.dev/docs/getting-started-mcp)
- click on `Install in VS Code` button
- It automatically routes to VS code --> Install it via `install in workspace` button
- should see `.vscode` folder within that should see `mcp.json` button