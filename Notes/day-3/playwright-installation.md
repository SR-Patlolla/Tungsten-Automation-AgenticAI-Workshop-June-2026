# Playwright Installation Guide

## Prerequisite
- node
- npm
- typescript

# Steps to install Playwright
- open a new empty folder in vs-code
- in terminal run the comman `npm init playwright@latest`
- Select `typescript` --> select `tests` folder --> select `no` for github actions --> select `yes` for playwright browser

# Execute sample test
- Run command `npx playwright test`